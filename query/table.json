{
    "validator": {
       "$jsonSchema": {
          "bsonType": "object",
          "required": ["name", "email", "message"],
          "properties": {
             "name": {
                "bsonType": "string",
                "description": "must be a string and is required"
             },
             "email": {
                "bsonType": "string",
                "description": "must be a string and is required"
             },
             "message": {
                "bsonType": "string",
                "description": "must be a string and is required"
             }
          }
       }
    }
 }
 